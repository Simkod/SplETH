export class ERCToken {
    address: `0x${string}`;
    symbol: string;

    constructor(address: `0x${string}`, symbol: string) {
        this.address = address;
        this.symbol = symbol;
    }
}
